class Solution {
public:
    int findComplement(int num) {
       int a= 0;int x =num;
       while(x > 0)
       {x=x >> 1; a++;}
       int dmax = pow(2, a)-1;
       return dmax - num;
    }   

};
